{
  "$schema": "https://json-schema.org/draft-07/schema",
  "title": "Chess Move",
  "description": "A move in a game of chess.",
  "type": "object",
  "properties": {
    "from": {
      "description": "The square the piece is moving from",
      "type": "string",
      "pattern": "^[a-h][1-8]$"
    },
    "to": {
      "description": "The square the piece is moving to",
      "type": "string",
      "pattern": "^[a-h][1-8]$"
    }
  }
}
